export const add = (a,b)=>(a+b).toFixed(2);
export const subtract = (a,b)=>(a-b).toFixed(2);
export const multiply = (a,b)=>(a*b).toFixed(2);
export const divide = (a,b)=>(a/b).toFixed(2);
export const opposite = (number) => -number;